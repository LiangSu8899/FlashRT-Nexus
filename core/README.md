# core/ — L1: the capsule protocol

The sovereign, zero-dependency capsule C ABI
(`include/capsule/capsule.h`) and its reference implementation.
Mechanism only: no scheduler, no policy, no loop, no thread, no lock,
no kernel knowledge. Frozen at 1.0 — changes here are additive-only
at seams (see CONTRIBUTING §2/§4).
